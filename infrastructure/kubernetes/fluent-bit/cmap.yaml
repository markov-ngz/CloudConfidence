apiVersion: v1
kind: ConfigMap
metadata:
  name: fluent-bit-aws-credentials
  namespace: fluent-bit
data:
  credentials: |
    [default]
    aws_access_key_id = 
    aws_secret_access_key = 
    region = eu-north-1
